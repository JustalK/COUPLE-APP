export interface ButtonImageProps {
	/**
	* The callback function executed after press event if fired
	**/
	callback: any;
	/**
	* The image of the button
	**/
	image: any;
	/**
	* True if the button has to be at the bottom of the page
	**/
	bottom?: boolean;
}
